# Advanced OpenPose Loader - Python Dependencies
# These are the dependencies needed to run the node outside of a full ComfyUI installation.

torch>=2.0.0
numpy>=1.24.0
Pillow>=10.0.0
safetensors>=0.4.0
tqdm>=4.60.0